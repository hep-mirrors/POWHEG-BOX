numevts 500000
ih1 1
ih2 -1

! To set for our internal pdf package (mlmpdf)
! ndns1 131
! ndns2 131

ebeam1 980d0
ebeam2 980d0

bornktmin 10d0

fixedscale -1d0

! To be set only if using LHA pdfs
lhans1 10000
lhans2 10000


! Parameters to allow or not the use of stored data
use-old-grid    1 ! if 1 use old grid if file pwggrids.dat is present (<> 1 regenerate)
use-old-ubound  1 ! if 1 use norm of upper bounding function stored in pwgubound.dat, if present; <> 1 regenerate

ncall1 80000 ! Number of calls to set up the importance sampling grid
itmx1 5      ! Number of iterations to set up the importance sampling grid
ncall2 20000 ! Number of calls to compute the integral and set up
             ! the upper bounds for the generation of the underlying
             ! Born kinematics;
             ! Look out: number of calls is ncall2*foldcsi*foldy*foldphi
itmx2 1      ! Number of iterations for the above

foldcsi 2
foldy 5
foldphi 5

nubound 300000 ! Number of calls to set up the upper bounding normalization
               ! for radiation
icsimax  1     ! <= 100, number of csi subdivision when computing the upper bounds
iymax    1     ! <= 100, number of y subdivision when computing the upper bounds
xupbound 2d0   ! increase upper bound for radiation generation

! OPTIONAL PARAMETERS

renscfact 1d0
facscfact 1d0
ptsupp 100d0
#bornonly 0
#ptsqmin    0.8    ! (default 0.8 GeV) minimum pt for generation of radiation
#charmthr   1.5    ! (default 1.5 GeV) charm treshold for gluon splitting 
#bottomthr  5.0    ! (default 5.0 GeV) bottom treshold for gluon splitting
#charmthrpdf  1.5  ! (default 1.5 GeV) pdf charm treshold
#bottomthrpdf 5.0  ! (default 5.0 GeV) pdf bottom treshold

#testplots  1      ! (default 0, do not) do NLO and PWHG distributions

#iseed 10001       ! Initialize random number sequence
#rand1     10      ! initialize random number sequence 
#rand2     10      ! initialize random number sequence 

#manyseeds 1       ! Used to perform multiple runs with different random
                   ! seeds in the same directory.
                   ! If set to 1, the program asks for an integer j;
                   ! The file pwgseeds.dat at line j is read, and the
                   ! integer at line j is used to initialize the random
                   ! sequence for the generation of the event.
                   ! The event file is called pwgevents-'j'.lhe

#withnegweights 1  ! (default 0) If set to one output also the negative weighted events
                   ! if you want to see where they end up.
 
